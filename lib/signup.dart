import 'package:flutter/material.dart';
class Signup extends StatelessWidget {
  final number,password;
  Signup({this.number,this.password});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
